# --- Core pipeline ---
networkx>=3.2
pydantic>=2.6
PyYAML>=6.0
jsonschema>=4.21

# --- Generated services / gateway / monolith ---
fastapi>=0.110
uvicorn[standard]>=0.29
flask>=3.0
httpx>=0.27
requests>=2.31

# --- Human-in-the-loop review UI ---
streamlit>=1.33

# --- Real LLM mode (optional at runtime; mock mode needs no key) ---
anthropic>=0.40

# --- Tests ---
pytest>=8.0
# imported directly by tests/test_strangler_integration.py and scripts/local_demo.py
# to drive the sync WSGI monolith from an async gateway; arrives transitively via
# httpx/fastapi, but a direct import belongs in the manifest.
anyio>=4.0
